info:
  name: Autenticar User
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/login"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: user@example.com
      - name: password
        type: text
        value: password

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
