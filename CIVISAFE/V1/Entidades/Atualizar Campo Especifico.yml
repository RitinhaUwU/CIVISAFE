info:
  name: Atualizar Campo Especifico
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/entities/11"
  body:
    type: json
    data: |-
      {
        "poc_name": "Sr. Guarda"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
