info:
  name: Remover Imagem
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/entities/{{id}}/image"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
