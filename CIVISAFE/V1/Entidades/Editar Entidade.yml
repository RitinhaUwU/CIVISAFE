info:
  name: Editar Entidade
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/api/v1/entities/1"
  body:
    type: json
    data: |-
      {
        "name": "GNR - Pedrogão"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
