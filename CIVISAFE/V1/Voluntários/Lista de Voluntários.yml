info:
  name: Lista de Voluntários
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/volunteers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
