info:
  name: Atualizar Campo Específico do Tipo de Estado
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/incidentStates/{{id}}"
  body:
    type: json
    data: |-
      {
        "terminates_incident": "false"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
