info:
  name: Editar Tipo de Estado
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/api/v1/incidentStates/{{id}}"
  body:
    type: json
    data: |-
      {
        "hex_color": "#00FF45"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
