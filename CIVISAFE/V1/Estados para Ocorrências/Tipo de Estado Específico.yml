info:
  name: Tipo de Estado Específico
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/incidentStates/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
