info:
  name: Apagar Ocorrência
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/incidents/82"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
