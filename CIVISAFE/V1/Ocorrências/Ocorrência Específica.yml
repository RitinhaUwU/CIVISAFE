info:
  name: Ocorrência Específica
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/incidents/81"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
