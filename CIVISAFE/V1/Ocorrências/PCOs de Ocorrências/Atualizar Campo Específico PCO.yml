info:
  name: Atualizar Campo Específico PCO
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/incidents/1/pco/61"
  body:
    type: json
    data: |-
      {
        "function_pco": "Oficial Operações Aérias"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
