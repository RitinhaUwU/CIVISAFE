info:
  name: Logs Específicos de uma Ocorrência
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/incidents/{{id}}/timeline"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
