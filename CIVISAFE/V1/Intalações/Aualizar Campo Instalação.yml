info:
  name: Aualizar Campo Instalação
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/facilities/1"
  body:
    type: json
    data: |-
      {
        "image": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
