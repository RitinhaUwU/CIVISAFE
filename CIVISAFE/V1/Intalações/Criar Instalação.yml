info:
  name: Criar Instalação
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/facilities"
  body:
    type: json
    data: |-
      {
        "name": "Junta de Freguesia da Sertã",
        "contact": "275418963",
        "email": "jfserta@example.com",
        "address": "Sertã",
        "image": null,
        "description": "teste teste"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
