info:
  name: Remover Imagem
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/facilities/{{id}}/image"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
