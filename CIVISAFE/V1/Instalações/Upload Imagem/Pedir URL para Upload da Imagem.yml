info:
  name: Pedir URL para Upload da Imagem
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/facilities/uploadUrl"
  body:
    type: json
    data: |-
      {
        "filename": "logotipo.png"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
