info:
  name: Remover Ficheiro
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/facilities/{{facility_id}}/documentos/{{file_id}}"
  auth: inherit

runtime:
  variables:
    - name: facility_id
      value: ""
    - name: file_id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
