info:
  name: Editar Entidade
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/api/v1/incidentPriorities/{{id}}"
  body:
    type: json
    data: |-
      {
        "description": "Por Averiguar"
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
