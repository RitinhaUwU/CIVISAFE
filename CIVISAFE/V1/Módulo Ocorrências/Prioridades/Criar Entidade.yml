info:
  name: Criar Entidade
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/incidentPriorities"
  body:
    type: json
    data: |-
      {
        "name": "P99",
        "description": "Estado Crítico",
        "hex_color": "#3b82f6"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
