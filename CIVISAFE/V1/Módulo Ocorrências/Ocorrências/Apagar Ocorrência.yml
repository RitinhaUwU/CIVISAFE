info:
  name: Apagar Ocorrência
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/incidents/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
