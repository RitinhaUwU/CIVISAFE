info:
  name: Apagar um Tipo de Estado
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/incidentStates/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
