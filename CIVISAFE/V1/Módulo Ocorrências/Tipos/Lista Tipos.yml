info:
  name: Lista Tipos
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/incidentTypes"
  params:
    - name: per_page
      value: "20"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
