info:
  name: Atualizar Utilizador
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/users/{{id}}"
  body:
    type: json
    data: |-
      {
        "locked": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
