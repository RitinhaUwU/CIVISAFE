info:
  name: Atualizar Utilizador
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/users/{{id}}"
  body:
    type: json
    data: |-
      {
        "locked": false
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
