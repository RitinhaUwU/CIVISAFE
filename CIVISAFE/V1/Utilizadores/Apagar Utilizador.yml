info:
  name: Apagar Utilizador
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/users/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
