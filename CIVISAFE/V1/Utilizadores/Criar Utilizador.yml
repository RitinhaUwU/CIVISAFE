info:
  name: Criar Utilizador
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
