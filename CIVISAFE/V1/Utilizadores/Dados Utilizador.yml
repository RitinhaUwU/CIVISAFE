info:
  name: Dados Utilizador
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
