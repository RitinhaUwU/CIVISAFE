info:
  name: Dados Utilizador
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/12"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
