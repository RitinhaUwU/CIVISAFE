info:
  name: Lista Prioridades
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/incidentPriorities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
