info:
  name: Criar Tipo
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/incidentTypes"
  body:
    type: json
    data: |-
      {
          "code": 12345,
          "species": "Fenómeno de Teste",
          "type": "Teste",
          "description": "Descrição do tipo de Teste."
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
