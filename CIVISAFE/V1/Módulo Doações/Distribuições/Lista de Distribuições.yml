info:
  name: Lista de Distribuições
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/donations/distributions/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
