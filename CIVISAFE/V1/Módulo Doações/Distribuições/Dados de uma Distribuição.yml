info:
  name: Dados de uma Distribuição
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/donations/distributions/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
