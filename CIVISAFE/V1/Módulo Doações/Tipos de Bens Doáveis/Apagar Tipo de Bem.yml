info:
  name: Apagar Tipo de Bem
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/donationGoodsTypes/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
