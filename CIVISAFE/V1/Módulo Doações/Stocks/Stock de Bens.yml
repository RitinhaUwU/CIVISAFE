info:
  name: Stock de Bens
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/donations/stock"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
