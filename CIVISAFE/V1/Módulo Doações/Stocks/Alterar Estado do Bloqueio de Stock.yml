info:
  name: Alterar Estado do Bloqueio de Stock
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/donations/stock/unlock"
  body:
    type: json
    data: |-
      {
        "state": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
