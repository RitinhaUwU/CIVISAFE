info:
  name: Obter Registo de Auditoria
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/donations/stock/audit"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
