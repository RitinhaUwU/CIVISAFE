info:
  name: Registar Alteração Manual ao Stock
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/donations/stock/audit"
  body:
    type: json
    data: |-
      {
        "adjustment_type": "remove",
        "category_id": 1,
        "quantity": 10,
        "reason": "lost",
        "obs": "Bem perdido na transferência entre armazéns"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
