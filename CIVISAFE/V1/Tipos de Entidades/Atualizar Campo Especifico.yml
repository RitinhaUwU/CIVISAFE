info:
  name: Atualizar Campo Especifico
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/entityTypes/1"
  body:
    type: json
    data: |-
      {
        "description": "Polícia de Segurança Pública"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
