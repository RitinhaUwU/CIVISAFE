info:
  name: Criar Tipo de Entidade
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/entityTypes"
  body:
    type: json
    data: |-
      {
        "name": "Bombeiros",
        "description": "Bombeiros"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
