info:
  name: Tipo de Entidades Especifica
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/entityTypes/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
