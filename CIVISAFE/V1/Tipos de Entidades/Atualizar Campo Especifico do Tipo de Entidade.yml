info:
  name: Atualizar Campo Especifico do Tipo de Entidade
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/entityTypes/{{id}}"
  body:
    type: json
    data: |-
      {
        "description": "Polícia de Segurança Pública"
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
