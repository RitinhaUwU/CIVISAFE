info:
  name: Editar Tipo de Entidade
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/api/v1/entityTypes/{{id}}"
  body:
    type: json
    data: |-
      {
        "name": "PSP",
        "description": "Polícia de Segurança Publica"
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
