info:
  name: Stock
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/donations/stock"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
